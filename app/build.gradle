
apply plugin: 'com.android.application'

android {
    namespace "com.ebookflix.app"
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.ebookflix.app"
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
}
